# telco_customer_churn_analysis
A production-ready Spark pipeline that ingests Telco data, trains a churn-prediction model, registers and serves the model, and produces a dashboard of key business metrics.
